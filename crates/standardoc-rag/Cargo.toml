[package]
name = "standardoc-rag"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Standardoc RAG layer — prose chunking, local embeddings (candle + BGE-small), chunk-to-symbol linking and relevance scoring. Sidecar SQLite store at .standardoc/rag.db."

[dependencies]
standardoc-ir       = { workspace = true }
rusqlite            = { workspace = true }
thiserror           = { workspace = true }
serde               = { workspace = true }
serde_json          = { workspace = true }
blake3              = { workspace = true }
walkdir             = { workspace = true }
candle-core         = { workspace = true }
candle-nn           = { workspace = true }
candle-transformers = { workspace = true }
tokenizers          = { workspace = true }
hf-hub              = { workspace = true }
# sqlite-vec: rejected day-1 in favor of brute-force cosine over BLOB
# vectors (sub-second for <100k chunks, zero native lib, simpler packaging).
# Schema unchanged ; swap-in is a perf optimization for later.

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
